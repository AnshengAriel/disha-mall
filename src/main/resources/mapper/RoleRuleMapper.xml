<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ariel.disha.mall.main.mapper.RoleRuleMapper">

    <select id="ruleList" resultType="com.ariel.disha.mall.consts.dto.RuleDto">
        SELECT
            *
            ,IF( rr.id, TRUE, FALSE ) AS `on`
        FROM
            rule
        LEFT JOIN ( SELECT id, rule_id FROM role_rule WHERE role_rule.role_id = #{vo.id} ) rr ON rule.id = rr.rule_id
        ORDER BY weight ASC
    </select>
</mapper>